<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".fragments.PasswordDetailsFragment">
<androidx.constraintlayout.widget.ConstraintLayout   android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TODO: Update blank fragment layout -->
<View
    android:id="@+id/mainView"
    android:layout_width="0dp"
    android:layout_height="100dp"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    android:background="@color/colorAccent">




</View>

    <ImageView
        android:id="@+id/clipArtDetails"
        android:layout_width="100dp"
        android:layout_height="0dp"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toBottomOf="@+id/mainView"
        app:layout_constraintLeft_toLeftOf="@+id/mainView"
        app:layout_constraintTop_toTopOf="@+id/mainView"
        app:srcCompat="@drawable/facebook"
        tools:ignore="VectorDrawableCompat" />

    <TextView
        android:id="@+id/pwTitleDetails"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:gravity="center|center_horizontal|center_vertical"
        android:text="Website"
        android:textSize="35dp"
        app:layout_constraintBottom_toBottomOf="@+id/mainView"
        app:layout_constraintLeft_toRightOf="@+id/clipArtDetails"
        app:layout_constraintRight_toRightOf="@+id/mainView"
        app:layout_constraintTop_toTopOf="@+id/mainView" />


</androidx.constraintlayout.widget.ConstraintLayout>
</layout>

    <!-- Slika sa id

          -->